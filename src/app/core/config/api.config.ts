export const API_CONFIG = {
  BASE_URL: '/api',
  ENDPOINTS: {
    AUTH: {
      LOGIN: '/Auth/login',
      PROFILE: '/auth/profile',
    },
    PROJECTS: '/projects',
  },
};

export const STORAGE_KEYS = {
  TOKEN: 'flux_token',
};
